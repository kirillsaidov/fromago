module menu.submenu.substance.quantity;

// the meta gtkd package
import meta;

void calculateSubstanceQuantity() {
	// calculateSubstanceQuantity window
	/*new FromagoDialog(mainWindow, "Substance Quantity Calc", 
		["Calculate", "Close"], 
		[ResponseType.ACCEPT, ResponseType.CANCEL], 
		(d) {
			d.addOnResponse(
				(r, d) {}
			);
		},
		new calculateSolutionConcentration()
	);*/
}




