module menu.submenu.calculator.substance.quantity;

// the meta gtkd package
import meta;

void calculateSubstanceQuantity() {
	// calculateSolutionConcentration window
	Window mainWindow = new Window("Calculate substance quantity", windowWidth, windowHeight, (MainWindow mw) {
		mw.add(new GUIBox(Orientation.VERTICAL, 0, (Box b) {
			// packing the element into guibox
			//...
		}));
	});
}




