module menu.submenu.calculator.solution.concentration;

// the meta gtkd package
import meta;

void calculateSolutionConcentration() {
	// calculateSolutionConcentration window
	Window mainWindow = new Window("Calculate solution concentration", windowWidth, windowHeight, (MainWindow mw) {
		mw.add(new GUIBox(Orientation.VERTICAL, 0, (Box b) {
			// packing the element into guibox
			//...
		}));
	});
}








